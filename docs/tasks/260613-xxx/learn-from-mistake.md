你刚才犯了一个错误。将这个教训提炼为一条规则，追加到 CLAUDE.md。

规则格式：
- "Never/Always + 具体行为 + 原因（一句话）"
- 例：`Never import from src/legacy/ — 已废弃，用 src/modules/ 替代。`

约束：
- 只追加，不重写其他内容
- 确认规则预期有效期 > 1 个月才添加
- 如果已有类似规则，合并而非重复
- 放在 CLAUDE.md 的"硬规则"章节下