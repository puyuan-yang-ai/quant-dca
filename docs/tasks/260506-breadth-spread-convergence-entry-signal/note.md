问题：原任务探索的是 Breadth 曲线间 spread 收敛是否能比单纯 breadth < 20 更有效地识别恐慌区买入机会。
影响：实验显示加入 c1 回升方向确认后成本优势更好，但最优停止条件样本偏少，稳定性不如 BreadthC3<20 和 VIX>30。
修复方案：推荐采用 B方向确认(无stop) 作为更稳健方案，即在 c1 < 20、spread(c5-c1) 收敛且 c1 回升时买入，不额外依赖停止阈值。